#ifndef DATAGENERATOR_H
#define DATAGENERATOR_H

#include <cstddef> // for size_t

namespace Generator {
    // This function's implementation is in DataGenerator.cpp
    void run(size_t n, size_t dim);
}

#endif // DATAGENERATOR_H